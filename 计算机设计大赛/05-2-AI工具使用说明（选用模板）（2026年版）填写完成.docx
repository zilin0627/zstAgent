# AI 工具使用说明

## 1. 项目概述

本项目是一个以侗族刺绣纹样为核心的 AI 辅助设计平台，旨在通过人工智能技术实现非遗文化的数字化活化与传承。平台集成了纹样图谱展示、智能文化导览、AI 辅助设计、文创产品展示等功能，形成了从文化理解到设计转化的完整工作流。

## 2. AI 工具使用情况

### 2.1 主要 AI 工具

| 工具名称 | 用途 | 技术实现 | 应用场景 |
|---------|------|---------|----------|
| RAG 检索增强 | 文化知识检索与问答 | LangChain + Chroma DB | 智能导览、设计工作台 |
| Agent 编排 | 多工具协同与决策 | LangChain Agent | 智能导览、设计工作台 |
| 大语言模型 | 自然语言理解与生成 | OpenAI API、DashScope API | 智能问答、设计建议 |
| 向量数据库 | 知识存储与检索 | Chroma DB | 知识库管理 |

### 2.2 工具使用流程

1. **知识准备**：将侗族刺绣相关的文献、图片等资料整理并嵌入到向量数据库中。
2. **用户交互**：用户通过平台界面提出问题或请求设计建议。
3. **智能处理**：Agent 接收用户请求，根据需要调用 RAG 服务检索相关知识，或直接使用大语言模型生成回答。
4. **结果呈现**：将处理结果以自然、友好的方式呈现给用户。

## 3. 核心功能模块

### 3.1 智能导览模块

**功能描述**：基于 RAG 技术和 Agent 编排，提供智能问答和文化讲解，帮助用户理解侗族刺绣纹样背后的文化寓意和工艺特征。

**AI 工具应用**：
- **RAG 检索增强**：从知识库中检索相关的文化知识和纹样信息。
- **Agent 编排**：根据用户问题类型，选择合适的回答策略和工具调用路径。
- **大语言模型**：生成自然、准确的文化解释和回答。

**使用示例**：
```python
# 智能导览模块核心代码
response = st.session_state.agent.execute_stream(prompt, context=context)
for chunk in response_stream:
    if chunk.startswith("[[THOUGHT]]"):
        # 处理思考过程
        pass
    else:
        # 处理回答内容
        response_message.append(chunk)
```

### 3.2 设计工作台模块

**功能描述**：提供 AI 辅助设计功能，用户可以基于传统侗族刺绣纹样进行创意设计和风格转化。

**AI 工具应用**：
- **RAG 检索增强**：检索与用户选择纹样相关的设计参考和文化背景。
- **Agent 编排**：根据用户的设计需求，调用相应的设计工具和模型。
- **大语言模型**：生成设计建议和创意方案。

**使用示例**：
```python
# 设计工作台模块核心代码
answer, citations, retrieval, confidence, system_notice = _run_direct_rag_request(prompt)
```

## 4. 技术实现细节

### 4.1 知识库构建

**数据来源**：
- 侗族刺绣相关文献和研究资料
- 真实绣片扫描样本
- 纹样分类和文化寓意资料

**构建流程**：
1. 收集和整理侗族刺绣相关资料
2. 对资料进行预处理和向量化
3. 将向量化数据存储到 Chroma DB 中
4. 定期更新和维护知识库

**代码示例**：
```python
# 知识库构建核心代码
# rag/vector_store.py
def build_vector_store():
    # 加载文档
    documents = load_documents()
    # 向量化
    embeddings = create_embeddings(documents)
    # 存储到向量数据库
    store_embeddings(embeddings)
```

### 4.2 RAG 检索增强

**实现方式**：
- 使用 LangChain 的 RAG 功能
- 结合向量数据库进行相似度搜索
- 对检索结果进行重排序和整合

**优势**：
- 提高回答的准确性和相关性
- 减少大语言模型的 hallucination
- 提供可追溯的知识来源

### 4.3 Agent 编排

**实现方式**：
- 使用 LangChain 的 Agent 框架
- 定义工具和工具使用策略
- 实现多步骤推理和决策

**优势**：
- 实现多工具协同工作
- 提高系统的灵活性和适应性
- 支持复杂任务的处理

## 5. 应用效果

### 5.1 智能导览效果

- **文化理解**：用户可以通过自然语言提问，获得关于侗族刺绣纹样的详细解释和文化背景。
- **交互体验**：系统能够理解用户的意图，提供个性化的回答和建议。
- **知识覆盖**：覆盖了侗族刺绣的历史、工艺、纹样分类、文化寓意等多个方面。

### 5.2 设计辅助效果

- **创意启发**：基于传统纹样提供创意设计建议，帮助设计师获得灵感。
- **风格转化**：支持将传统纹样转化为现代设计风格，适应不同的应用场景。
- **设计指导**：提供专业的设计建议和技术支持，提高设计效率和质量。

## 6. 技术创新点

1. **知识增强**：构建了专门的侗绣纹样知识库，通过 RAG 技术实现精准的文化知识检索和智能问答。
2. **AI 编排**：使用 LangChain 实现 Agent 工作流编排，结合多种工具和模型，提供更智能的交互体验。
3. **全链路设计**：从文化来源、纹样理解、数字转化到文创产品化和场景落地，形成完整的价值链条。
4. **用户友好**：通过直观的界面和智能交互，降低用户理解和使用门槛。

## 7. 后续发展计划

1. **知识库扩展**：继续收集和整理更多侗族刺绣相关资料，丰富知识库内容。
2. **模型优化**：优化大语言模型的使用，提高回答的准确性和相关性。
3. **功能扩展**：增加更多设计工具和交互方式，提升平台的实用性和趣味性。
4. **应用场景拓展**：探索更多应用场景，如教育、文旅、文创等领域的深度应用。

## 8. 总结

本项目通过整合 AI 技术和侗族刺绣文化，实现了非遗文化的数字化活化与传承。平台不仅提供了丰富的纹样展示和文化导览功能，还通过 AI 辅助设计功能，促进了传统纹样在现代设计中的应用。未来，我们将继续优化平台功能，拓展应用场景，为非遗文化的保护和传承做出更大的贡献。